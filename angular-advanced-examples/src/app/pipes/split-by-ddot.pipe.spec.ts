import { SplitByDDotPipe } from './split-by-ddot.pipe';

describe('SplitByDDotPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByDDotPipe();
    expect(pipe).toBeTruthy();
  });
});
